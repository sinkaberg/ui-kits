{
  "name": "@sinkaberg/ui-kits",
  "version": "1.0.0",
  "private": true,
  "description": "Sinkaberg UI kits — the public, hotlinkable source of truth for on-brand HTML (tokens, components, fonts, layouts).",
  "type": "module",
  "scripts": {
    "assemble": "node tools/assemble.mjs",
    "lint": "node tools/adherence-lint.mjs",
    "check": "node tools/assemble.mjs --check && npm run lint && npm test",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
